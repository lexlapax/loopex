<a id="concept"></a>
## Concept

Technical depth: [Headless consumer mechanics](M4-technical.md#technical-depth).

<!-- loopex:plan-concept-envelope:start -->
## Normative Concept Envelope

<a id="concept-plan-purpose"></a>
### Purpose

Let an independent program use the local skills and core repairs M3 proves, then
add durable policy questions and bounded artifact retrieval. Select an
admitted skill, drive a coding session, answer a durable policy question,
observe progress and committed results, retrieve an artifact, and recover the
same durable session after process loss. Ship one foreground stdio JSONL server
and a useful TypeScript consumer over the public facade.

M4 first implements durable interactions in core and bounded retrieval at the
ArtifactStore/facade boundary, then adds an external consumer, schemas and
delivery mechanics. It reuses M3 resource admission, operator selection, launch
configuration and recovery. The protocol never owns a second policy engine or
resource resolver. The floor refresh supports its standard-library JSON codec.
Keeping this milestone separate lets M3 deliver skills without those protocol
prerequisites; M4 proves its new shared contracts locally before wire mapping.

<a id="concept-plan-outcomes"></a>
### Outcomes

| # | Outcome | Evidence class | Gate selector |
| --- | --- | --- | --- |
| 1 | A separate process initializes an exact experimental generation, schema digest and limits before any mutation | Raw-byte process probe and schema vectors | `apps/loopex_app_server/test/initialization_test.exs` |
| 2 | Session commands, admissions, snapshots and durable events have the same identities and meaning through facade and wire | Differential conformance and replay properties | `apps/loopex_app_server/test/session_mapping_test.exs` |
| 3 | Core durably suspends/resumes policy interactions; clients use these and M3 resource selection without authority from client content | Reducer/Store fault tests, mapping negatives and real effects | `apps/loopex/test/interaction_lifecycle_test.exs`, `apps/loopex_app_server/test/foundation_mapping_test.exs` |
| 4 | Shared artifact range retrieval, progress delivery and slow-reader handling remain bounded and truthful | ArtifactStore integrity/allocation, backpressure and process-lifetime tests | `apps/loopex_store_local/test/artifact_range_test.exs`, `apps/loopex_app_server/test/delivery_bounds_test.exs` |
| 5 | The TypeScript consumer completes skill → approval → tool → artifact → restart using the shipped server and real local Store/executor | Integrated client workflow plus attended real-provider demonstration | `apps/loopex_app_server/test/external_workflow_test.exs` |
| 6 | Independent language clients agree on the exact vectors and fail coherently on malformed input, process loss and incompatible versions | Elixir/Python/TypeScript conformance and exact-source package/build proof | `apps/loopex_protocol/test/public_schema_conformance_test.exs` |

Technical depth: [Evidence mapping](M4-technical.md#technical-plan-evidence).

<a id="concept-plan-scope"></a>
### Scope

The server owns framing, connection-local correlation, delivery pressure and
foreground process lifetime. Core owns durable session truth. Hosts supply
workspace, identity, policy, provider configuration and project resources at launch;
client requests cannot choose modules, grant profiles, roots or credentials.

Initialization precedes mutation. Request identity is ephemeral correlation;
command identity provides durable idempotency; interaction identity survives
transport loss. Creation does not silently attach. Attach returns a snapshot
and cursor before live delivery. Command admission is distinct from completion,
and progress is distinct from committed events. Lost progress falls back to
snapshot/event truth. Artifact references stay opaque and retain separate
object/use identity.

The TypeScript example is a small usable consumer, not just an echo/vector
fixture. It lists the admitted catalog, explicitly selects a skill, submits a
task, presents and answers an interaction, follows the resulting tool execution,
saves a bounded artifact stream and resumes after abrupt server loss. A second
workflow tests graceful EOF/cancellation separately so a cancelled interaction
is never falsely described as recoverable pending work.

All new surfaces remain experimental, with exact schema/generation agreement
and no mixed-generation promise. M4 adds versioned core interaction records;
new readers replay M3 histories and old readers refuse new records before effects.
Rollback retains the old root/binary pair, not just an older server executable.
The proposed floor refresh requires separate acceptance and every affected Closed
gate transaction before M4 binds it. Publication and public freezes remain
separate decisions.

Technical depth: [Prerequisites](M4-technical.md#technical-plan-prerequisites).

Technical depth: [ownership](M4-technical.md#technical-plan-ownership).

Technical depth: [compatibility](M4-technical.md#technical-plan-compatibility).

Technical depth: [rollback](M4-technical.md#technical-plan-migration).

Technical depth: [packaging](M4-technical.md#technical-plan-packaging).

Technical depth: [minimalism](M4-technical.md#technical-plan-minimalism).

<a id="concept-plan-non-goals"></a>
### Non-Goals

No second resource manager, policy engine, interaction reducer or session loop.
No daemon, sockets, persistent concurrent-client residency, controller takeover,
trusted extension activation, marketplace, hosted skill API, mid-session model
switch, ACP mapping, protocol freeze or implicit publication. Those later
capabilities retain their vision barriers and require their own evidence.

Technical depth: [Acceptance boundaries](M4-technical.md#technical-plan-prerequisites).
<!-- loopex:plan-concept-envelope:end -->

## Workstreams

Settle floor/holder transactions and DTO/core contracts against M3's actual
facade. Prove core interaction and ArtifactStore range paths, then join a thin
server/TypeScript workflow before method breadth. Reuse M3's focused checkpoints,
clause witnesses and integrated self-audit; full inherited checks remain at
contract moments and scheduled CI. No second resource or authority engine.

This is the mutable, unopened M4 draft revised under the maintainer's current
request. It has no register row or implementation authority. Its previous bytes
remain in Git at `d0699b6dc636c7e74b95082d0d8241968381927d`.


This draft is refreshed conceptually against M2 main
`b637873ddc39542ec27add71015b46a4f7c7f80e`; its implementation base will be the
exact integrated M3 closure, not this M2 source. It must be refreshed again and
reviewed at opening. The current request permits revising this draft in place;
historical seed archive material remains immutable.

## Progress and Evidence

| # | State | Evidence |
| --- | --- | --- |
| 1 | Open | Superseded scaffolding retained in Git history; reconstruct only when opening |
| 2 | Open | Refresh exact facade mapping after M3 integration |
| 3 | Open | Reuses M3 resources; M4 core interaction contract not implemented |
| 4 | Open | M4 range port and wire delivery bounds not implemented |
| 5 | Open | TypeScript workflow is required before breadth |
| 6 | Open | No compatibility or client execution claim yet |

## Governance Records

| Decision | Authority | Authority evidence | Bound bytes |
| --- | --- | --- | --- |
| Acceptance | — | — | — |
| Closure | — | — | — |
