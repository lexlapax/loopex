# M1 Toolchain Matrix

The source candidate intentionally carries no capture claims. After the exact
candidate is committed and remains unchanged, the Darwin floor/current and
Linux-current M1 captures plus inherited M0 floor/current proofs replace this
placeholder in the direct evidence-only child commit required by the M1 gate.
