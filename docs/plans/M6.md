<a id="concept"></a>
## Concept

Technical depth: [Prerequisites, contracts, evidence, compatibility and packaging](M6-technical.md#technical-depth).

<a id="concept-plan-purpose"></a>
### Purpose

Technical depth: [Prerequisites and acceptance points](M6-technical.md#technical-plan-prerequisites).

**M6 is the minimal runnable Loopex, the `0.3.0` candidate.** Its product
question is:

> Can Loopex be run as a minimal standalone agent loop — called from an
> Elixir host in one function call, from a shell or from another agent in one
> command — and still be the full durable Loopex when that is what the caller
> asks for, all over one kernel?

Loopex already is a working coding harness, but only in its heaviest form.
Every composition needs:

- a state root on disk and the local store;
- a provider companion BEAM with a built worker artifact and digests;
- a named policy;
- one pinned model.

The command needs a subcommand, `--policy`, `LOOPEX_HOME` and the companion
build. It has no model flag and no machine-readable output, and a failed run
exits 0. Nothing lets a host ask one question and get one answer.

M6 does not write a second, simpler loop: the vision forbids a surface owning
an alternate loop. It composes the unchanged kernel with lighter parts behind
its existing ports:

- a memory store;
- an in-process model adapter;
- the local executor on a temporary root.

The result is three instances of one Loopex:

| Instance | Called by | Composed with | Promises |
| --- | --- | --- | --- |
| **Embedded, ephemeral** | An Elixir host: `LoopexComposition.Ephemeral.run/2`, `start_session/1`, `ask/3`, `history/1`, `stop_session/1` | The memory store, the in-process model adapter, the local executor | A whole multi-turn session inside one VM; nothing persists; no recovery claim |
| **Standalone command** | A shell, CI or another agent: `loopex ask "…"`, or its alias `loopex -p "…"` | The ephemeral profile by default; the durable profile with `--state-root` | The answer on stdout, or one JSON object when asked; an exit status that reports the run's outcome |
| **Full Loopex** | Operators and multi-client work | The durable local store, the provider companion, the daemon | Everything M1 to M5 proved, unchanged |

The embedded API lives in `loopex_composition`, not in core. Assembling the
memory store, the adapter and the executor is exactly the work the dependency
direction keeps out of core, and M6 leaves core's runtime library unchanged.
The closure tooling's two Mix tasks join core's existing development tasks.

**Why this rung, and why now.** A second person, or a second agent, can use
Loopex only once it can be called without assembling the durable stack. Every
comparable minimal harness offers exactly that: one library call, one command
and a small fixed tool set. The installed operator milestone, drafted as M7,
depends on it, because its configuration and lifecycle commands sit on top of
the profiles and the command M6 fixes.

The new sequence, recorded here and projected into the roadmap on acceptance:

- **M6:** this milestone, the minimal runnable Loopex.
- **M7:** the installed durable operator.
- **M8:** the store engine successor.
- **M9:** the governed extension runtime.

M7, M8 and M9 are drafted in [docs/drafts](../drafts/README.md) and are
refined as M6 proceeds.

**The M5 baseline, and permission to rework it.**
- **Baseline:** M5 closed on 2026-09-26 at the tested implementation `fe020e24`
  (administrative closure `3f81b048`, tagged `v0.2.0`, integrated on `main`).
- **Rework permission:** the maintainer authorized M6, on the same day, to rework
  code delivered by M1 to M5 where the minimal profile needs it. M6 uses that
  permission for a small set of changes, each additive and each held to the
  unchanged M5 suites:
  - the durable composition gains model, bound, sampling and active-tool options;
  - the companion's pure request mapping is shared;
  - the memory store is promoted from a test wrapper;
  - three read-only tools are defined;
  - the executor clears every provider credential name from tool processes;
  - the release check gains three rows.
- **What the permission does not cover:** the vision's non-negotiables. Those
  are dependency direction, one serial owner, durability truth, plain boundary
  data, and the durable profile's credential isolation.

<a id="concept-plan-outcomes"></a>
### Outcomes

Technical depth: [Evidence obligations and mapping](M6-technical.md#technical-plan-evidence).

| # | Outcome | Proved by |
| --- | --- | --- |
| 1 | **Embedded ephemeral API.** `LoopexComposition.Ephemeral`: `run/2`, `start_session/1`, `ask/3`, `history/1`, `stop_session/1` over the unchanged kernel. A required `:policy` module (composition ships no policy of its own); a `:model` string; `:tools` (`:coding` or `:read_only`); `:skills` (directories); `:system`, `:cwd`, `:max_steps`, `:deadline_ms`, `:max_tokens`. A completed run returns `{:ok, result}`, and every other outcome returns `{:error, {:run, outcome, details}}` | A host with no state root and no companion build gets an answer from one call and holds a multi-turn conversation; the step limit returns `bound_reached` on `max_turns`; a tool failure reaches the next model call; a deferring policy returns `interaction_pending` and the next ask is refused until that run finishes; each ask returns its own run's ending; stopping aborts an open run and confirms the executor's process groups are gone before removing the root, and the caller exiting does the same |
| 2 | **In-process model adapter.** It covers Ollama (no key; the default model when none is named), OpenAI, Anthropic and OpenRouter, each with its own credential variable. It implements the unchanged model port with native tool calls and streamed deltas, over the request mapping it shares with the companion | Its own mapping and error suites and the model streaming conformance suite pass; real-provider lanes run against a local Ollama model and one hosted provider; a canary credential never appears in any plane or captured log on the stream-start failure, stream-task crash and provider-error paths; no provider variable reaches a tool process; the companion's suites pass unchanged; an independent security review of the credential path names the tested SHA |
| 3 | **Memory store and ephemeral composition.** `Loopex.Store.Memory`, the conformance-proven in-memory store state promoted to library code, and one composition that builds the ephemeral profile: temporary root, working-directory workspace, named policy, model, skill directories, and no artifact store | The store conformance suite passes against the shipped module; the profile refuses without a policy and leaves no file after it stops |
| 4 | **Standalone command for shells and agents.** `loopex ask`, and its alias `loopex -p`: `--model`, `--output text|json`, `--skill DIR`, `--tools`, a required `--policy` (`allow-all`, `shell-allowlist`, `refuse-all`), `--cwd`, `--max-steps`, a prompt from words or stdin. No home required; `--state-root` selects the durable profile; one exit status per run outcome | A shell with no `LOOPEX_HOME` gets only the answer on stdout; `--output json` yields exactly one parseable object; each outcome exits with its own status; another agent drives both `loopex -p` and the existing JSON-lines app server and reads the results |
| 5 | **Full Loopex unchanged.** The durable composition, the companion, the daemon and both protocol generations behave exactly as closed M5, and core's runtime library is unchanged | The M5 suites and release lanes pass unchanged; `git diff v0.2.0 -- apps/loopex/lib` is empty outside `apps/loopex/lib/mix/`; the durable composition's active tools are still the four coding tools; the dependency budget is unchanged |
| 6 | **Closure and release tooling in the repository.** Repository commands for the milestone guide's pre-tag proofs, the floor lane's preconditions and the attended release cases, in the project toolchain only | Each command reproduces the M5 closure's checks on a fixture, with a failing case for each; M6's own closure runs every proof with them from a clean checkout |

**The single acceptance demonstration**, on macOS and on Linux from a clean
checkout, with no `LOOPEX_HOME`, runs four things in turn:

1. `LoopexComposition.Ephemeral.run/2` answers a coding question against a
   local Ollama model with the four coding tools and one skill directory.
2. `loopex -p … --output json` does the same from a shell, and exits with the
   `failed` status when the run is made to fail.
3. A second process delegates a task to `loopex -p` and reads the JSON result.
4. The same prompt with `--state-root` runs under the durable profile and is
   resumable exactly as in M5.

<a id="concept-plan-scope"></a>
### Scope

Technical depth: [Ownership and rejoin](M6-technical.md#technical-plan-ownership).

Technical depth: [The embedded API contract](M6-technical.md#technical-plan-api).

Technical depth: [The in-process model adapter contract](M6-technical.md#technical-plan-adapter).

Technical depth: [The memory store contract](M6-technical.md#technical-plan-store).

Technical depth: [The composition contract](M6-technical.md#technical-plan-composition).

Technical depth: [The command contract](M6-technical.md#technical-plan-command).

Technical depth: [The read-only tools contract](M6-technical.md#technical-plan-tools).

Technical depth: [The closure tooling contract](M6-technical.md#technical-plan-closure-tooling).

Technical depth: [Ownership and failure at every boundary](M6-technical.md#technical-plan-failures).

**The embedded API.** One module in `loopex_composition` that composes the
ephemeral profile and follows the session through core's existing public
facade. It returns the last assistant text on `completed`, and the
`run.finished` details for every other outcome. A policy that defers is
surfaced as a pending interaction and is not answered for the host.

**The model adapter.**
- **Placement:** an in-process ReqLLM adapter in `loopex_llm_reqllm`, beside
  the companion adapter, sharing the companion's pure request and reply mapping.
- **Call options:** it always passes the credential explicitly, disables retries
  and bounds its wait by the request deadline.
- **Host hygiene:** it turns off ReqLLM's automatic `.env` loading and
  unverified-model warning before ReqLLM starts, and uses an inline model
  specification so no catalog lookup occurs.
- **Selection:** composition selects exactly one adapter per runtime.

**The store.** `Loopex.Store.Memory`: the shipped pure store state that the
local store replays, behind a small supervised process, the same logic the
conformance suite already proves in memory. There is no artifact store in the
ephemeral profile, so oversized tool output is truncated with the existing
notice.

**Composition.**
- The ephemeral profile as above.
- On the durable `LoopexComposition.start/1`: optional `:model`, `:bounds`,
  `:sampling` and `:active_tools`, whose defaults reproduce M5 exactly.
- `ResourcePacks.read_directories/2` builds a resource manifest from named skill
  directories under the existing validation.

**The command.**
- `loopex ask`, and its `-p` alias, in `loopex_cli`: text or JSON output and the
  prompt from words or stdin.
- **Exit map:** `0` for completed, `1` for a refusal before the run, `2` to `5`
  for failed, bound reached, outcome unknown and cancelled, `6` for no ending
  within the wait, `7` for an unanswered deferred interaction, and the existing
  `130` interrupt.
- **Policy names:** `ask` maps `allow-all`, `shell-allowlist` and `refuse-all`
  to the reference CLI's policy modules.
- **Credential and crash output:** `ask` turns the logger off, renders its own
  lines, and runs with crash dumps disabled, so ReqLLM's failure paths cannot
  write request data.
- **Dispatch:** `ask` is an offline form, and `--daemon` is not accepted.
- **`run`** keeps its behaviour.

**Tools.** `grep`, `find` and `ls` join the local executor as read-only,
workspace-confined, byte-bounded in-VM tools and form a `:read_only` preset.
They are active only where a composition names them. The durable default stays
`read`, `write`, `edit` and `bash`.

**Closure tooling.** Two Mix tasks and three shell scripts:
- the five-path confinement proof;
- hostile-umask archive staging;
- the archive-identity comparison;
- the floor lane with its open-file and signal preconditions;
- an attended-release runner using the platform's `script(1)`.

They are tested against a fixture repository and replace every script the M5
closure kept outside the repository.

**Documentation.** A getting-started path for each instance, and the operator
and developer pages the profiles change.

**Version.** `VERSION` moves from `0.2.0` to `0.3.0` in the tested candidate.

<a id="concept-plan-non-goals"></a>
### Non-Goals

Technical depth: [Deferrals and named limitations](M6-technical.md#technical-plan-limitations).

- **The M7 draft's scope:** no installed release archive, no saved
  configuration file, no default home, no provider profiles, no per-provider
  durable credentials, and no lifecycle commands beyond `ask`.
- **The M8 draft's scope:** no store engine, migration, format marker, backup
  or restore.
- **The M9 draft's scope:** no extension activation.
- **Model features:** no model switching within a run.
- **Protocol:** no protocol change.
- **Integrations:** no MCP client or server.
- **Loop features:** no streaming UI, no parallel tool fan-out, no multi-agent
  trees, and no progressive skill disclosure.
- **Persistence:** no persistent memory for the ephemeral profile, and no
  migration between profiles.
- **Publication:** no Hex publication; that is a separate later decision.
- **Core:** no change to core's runtime library or dependency list.
- **Credential isolation:** no weakening of the durable profile's credential
  isolation.

**Accepted limitations**, named in the technical plan:
- The ephemeral credential lives in the host VM.
- A host that already started ReqLLM with dotenv on has already loaded its
  `.env`.
- A hard VM kill leaves the empty temporary root.
- The durable `--model` must match its single credential.
- `ask` answers no deferred interaction.
- The JSON result carries no usage or model identity, because those are not
  public events.
- A library host owns its own logger and crash-dump configuration for ReqLLM's
  failure paths.
- An ephemeral session's memory grows with its history.

<a id="concept-plan-decisions"></a>
### Design Decisions

Technical depth: [Prerequisites and acceptance points](M6-technical.md#technical-plan-prerequisites).

One decision is proposed with this plan:

| Decision | What it settles | Outcomes that wait on it |
| --- | --- | --- |
| [ADR 0039](../adr/0039-ephemeral-embedded-profile.md#concept) | Two composition profiles over one kernel. The ephemeral profile covers the memory store's truth statement, the in-process adapter with its credential and host-hygiene rules, and the default Ollama model. The durable profile keeps ADRs 0019 and 0034 unchanged. Neither profile has a default authority | 1, 2, 3, 4 |

**Decisions settled here rather than in an ADR,** because each is host behaviour
or internal structure that no accepted decision binds:

- The embedded API lives in `loopex_composition` and follows the session only
  through core's public facade.
- The `ask` command and its `-p` alias are a new subcommand beside `run`. The
  existing parser grammar and `run`'s behaviour are unchanged.
- `ask`'s exit map (above) uses values below the daemon's 65–111 range and
  distinct from 127 and 130. `run` and the daemon keep theirs.
- `--output json` writes exactly one object at the end, with schema
  `loopex.ask/1`, carrying:
  - the session and run ids, the outcome and the final text;
  - each tool's id and outcome;
  - the outcome's details.

  Standard error carries the same tool and ending lines as `run`.
- `refuse-all` becomes a selectable policy name for `ask`.
- Skill directories passed by path are admitted by that act. Project-local
  discovery keeps its interactive prompt.
- The new read-only tools are defined for every composition and active only
  where a composition names them.

**Prerequisites that are not ADRs.** M5 closure is satisfied (2026-09-26). The
M7, M8 and M9 drafts are refined, not accepted, during M6.

<a id="concept-plan-verification"></a>
### How Each Outcome Is Verified

Technical depth: [Evidence obligations and mapping](M6-technical.md#technical-plan-evidence).

Verification is the current suite plus the two check commands.

**The fast check** proves, credential-free:
- the embedded API against a scripted model;
- the memory store's conformance and the ephemeral composition;
- the adapter's mapping, options, errors and host hygiene against a scripted
  ReqLLM transport;
- the canary credential's absence;
- the command grammar, output modes and exit map;
- the read-only tools and skill directories;
- the closure tooling against fixtures.

**The release check** grows from nine real-provider rows to twelve:
- two ephemeral-profile real-provider lanes, one against a local Ollama model and
  one against the hosted provider the release credential serves;
- an agent-delegation lane.

A reachable local Ollama model becomes a stated release-host precondition. If it
is missing, that is evidence unavailable, not a skip. The credential-free lanes
clear every provider credential name.

**Also required:**
- The M5 suites and lanes run unchanged, and core's runtime library diff from
  `v0.2.0` is empty.
- An independent read-only security review of the ephemeral credential path
  names the tested SHA.
- Closure follows the two-commit procedure, run with outcome 6's repository
  commands.

<a id="concept-plan-rollout"></a>
### Rollout and Compatibility

Technical depth: [Compatibility](M6-technical.md#technical-plan-compatibility).

Technical depth: [Migration and rollback](M6-technical.md#technical-plan-migration).

Technical depth: [Packaging](M6-technical.md#technical-plan-packaging).

`0.3.0` is a minor release under the 0.x policy.

**New experimental surfaces:**
- the embedded API;
- `ResourcePacks.read_directories/2`;
- the `ask` command;
- the `provider:model` strings.

**Additive reworks of M1 to M5 code:**
- the durable composition's four options;
- the shared companion mapping;
- the promoted memory store;
- three defined tools;
- the executor clearing every provider credential name;
- three release rows.

It changes no durable byte, protocol, executor contract or core runtime library, and
every M2 to M5 invocation keeps working. There is no new application and no new
dependency. On acceptance the roadmap names this rung the `v0.3` candidate and
moves the later rungs by one label, as the drafts record.

## Workstreams

Four workstreams with non-overlapping ownership and one integrator, rejoining
in the order the technical plan fixes:

1. model adapter and memory store;
2. composition;
3. command and tools;
4. closure tooling and documentation.

The single acceptance demonstration is written first as a failing script and
turned green as each workstream lands.

## Progress and Evidence

The register in [docs/plans/README.md](README.md) owns M6's lifecycle state,
and each transition records exactly one thing, as the M5 plan states for the
two-commit closure procedure this plan inherits. Every row reads `Open` while
the register state is `Open`.

| # | State | Evidence |
| --- | --- | --- |
| 1 | Open | No synchronous API exists; a host drives `create_session`, `attach`, `command` and a polled `next_event` by hand; `next_event` never blocks |
| 2 | Open | ReqLLM runs only inside the provider companion, with a built worker and digests and no in-VM fallback; composition hard-codes `anthropic:claude-haiku-4-5`; one credential variable |
| 3 | Open | The conformance-proven in-memory store exists only as a test wrapper over `Loopex.Store.Local.State`; every composition needs a state root |
| 4 | Open | The command needs a subcommand, `--policy`, `LOOPEX_HOME` and the companion; it has no `--model` or JSON output; a failed run exits 0 |
| 5 | Open | The M5 lanes are green at `v0.2.0` |
| 6 | Open | The M5 closure's pre-tag proofs, floor-lane preconditions and attended-release driver ran as scripts kept outside the repository |

**Carried from M5, to be disposed before this plan is accepted.** The
[M5 closure disposition](../developer/agent-context-map.md#disposition-m5-closure-2026-09-26)
hands this plan five items besides the closure tooling, which outcome 6 now
owns. None of them changes an M6 outcome until the maintainer decides it:

- **ADR 0028 divergence (deferred beyond M5).** Accepted ADR 0028 counts artifact-transfer concurrency per connection and adds 1 GiB of cumulative work per connection. The implementation since `0.1.0` counts two transfers per attachment and enforces no allowance. The fix is either to amend the ADR to the implemented unit or to implement the per-connection unit and its allowance. The divergence is disclosed on the operator and developer pages.
- **P1: the charter's exception list.** Name the three cross-audience developer references, root `CLAUDE.md`, `.claude/agents/*.md` and `test/fixtures/` in the charter's exception list, which the status checker already admits.
- **P2: when the release check runs.** Reconcile `AGENTS.md` ("once for the candidate before closure") with the verification selection table, which routes provider changes to it before merge.
- **P3: where the pre-merge fast check runs.** Let the verification guide's Change row say "or locally from the same bytes before the merge", as `AGENTS.md` does.
- **P4: the closure-candidate branch.** Let the milestone guide name the milestone branch the plan designates, not only `main`.

## Governance Records

| Decision | Authority | Authority evidence | Bound bytes |
| --- | --- | --- | --- |
| Acceptance | — | — | — |
| Closure | — | — | — |
