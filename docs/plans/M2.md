<a id="concept"></a>
## Concept

Technical depth: [Milestone mechanics](M2-technical.md#technical-depth).

<!-- loopex:plan-concept-envelope:start -->
## Normative Concept Envelope

<a id="concept-plan-purpose"></a>
### Purpose

Give an operator a coding agent they can use from their own terminal. They run
`loopex` inside a Git repository, describe a change in ordinary words, and watch
the session read files, edit them, and run commands until the work is done. The
answer arrives as it is produced rather than after the run. The model sees the
whole conversation — the operator's prompt, its own prior messages, and the real
output of every tool it ran — so the loop runs as many turns as the task needs
instead of stopping after the second. The operator can redirect the run while it
is working, or queue the next piece of work behind it. Host policy can refuse a
call and the operator sees the refusal. Ctrl-C stops the owned work and reports
what actually happened. Tomorrow, `loopex sessions` finds today's work and
`loopex resume` continues it.

M2 is the foreground harness. One operator, one terminal, one process, one
durable session on the local disk. Everything an operator asks for is asked for
in that process and answered in that process.

M1 proved the durability kernel underneath that experience: a session survives
an untrappable kill, reconciles its effect without dispatching it again, and
loses no acknowledged fact. It did not prove a loop. Its loop is hardwired to
two turns, sends the model a synthesized string instead of the conversation,
never streams, and exposes two demonstration tools. This milestone turns that
kernel into a tool a developer actually uses.

The same source and gate execute on Darwin under both ADR 0002 validation pairs
and on Linux under the current pair. That is milestone execution evidence, not
an operating-system support freeze.

<a id="concept-plan-outcomes"></a>
### Outcomes

| # | Outcome | Evidence class | Gate selector |
| --- | --- | --- | --- |
| 1 | A single prompt runs the task to completion: the loop continues while the model asks for tools and ends when it stops, every model request carries the committed conversation history including the original prompt, the model's own prior assistant messages, and the real output of each tool call, each turn dispatches exactly the canonical request bytes and digest committed before that dispatch, each staged request carries the complete tool-definition bytes and generation triple that make it reconstructible from the journal alone, the committed absolute deadline bounds every operation the run owns rather than only the gaps between turns, reaching the supervised model call and every executor job the run dispatches with no independent per-call or per-tool timeout that can outlast it, and each of the three declared bounds — maximum turns, cumulative token budget, and wall-clock deadline — commits `bound_reached` naming the bound and the observed value, with the accounting source retained beside it before a provider call is made, a reached deadline committing its `bound_reached(:deadline)` only after every owned operation has reached a validated terminal fact and every owned process tree has been confirmed cleaned, and `outcome_unknown` taking precedence wherever effect or cleanup truth cannot be proved, so a deadline is not a guaranteed clean stop, with no implicit sampling default anywhere; a retry of one tool operation keeps the stable `operation_id` that identifies it across attempts while carrying its own attempt-bound request digest, and the derived per-attempt deadline is carried beside that digested request rather than inside it; and a turn cancelled or cut short by the deadline is charged its request bytes plus that turn's committed output allowance in full so that aborting every turn is not the cheapest way to stay inside a budget | Loop property tests, committed-history replay, deadline-race ordering, accounting properties, and bound-by-bound termination | `apps/loopex/test/agent_loop_test.exs` |
| 2 | The operator sees the answer as it is produced: text, reasoning, tool-call, and tool-progress deltas are bounded plain data on the progress plane and arrive while the operation producing them is still incomplete; every delta and both closure items carry an opaque `stream_domain_id` naming the one attempt that produced them, which the coordinator derives from committed identity and which an adapter never supplies and an executor event never carries; the two kinds of domain — one per model attempt and one per executor operation attempt, each closed exactly once by its own content-free item, an abandoned domain included, so an operator is never left unable to tell an abandoned stream from one still running — make loss detectable rather than silent, and sequence continuity, count agreement, and closure are evaluated within one `stream_domain_id` and never across a turn; a provider retry and a retried executor attempt each open a new domain, so several domains under one turn are the normal shape of a retried turn rather than a fault; an adapter's deltas replay byte-identically to the reply it returned, the committed assistant message is built from that reply and never assembled from deltas, a cancelled stream commits no assistant message at all, and an adapter that cannot stream is conformant and says so | Shared model-port streaming conformance, temporal observation, reconstruction and domain-scoped loss-detection properties, retry-domain cases, and cancellation negatives | `apps/loopex_llm_reqllm/test/streaming_conformance_test.exs` |
| 3 | The operator can redirect a run and queue the next one: a prompt starts a run only while the session is settled, a steer names one active run and joins it after the current tool batch and before the next model request, a steer is recorded applied only when a committed request actually carried it, a follow-up starts a new run only after the active run and its steering settle, a steer that arrives too late commits unapplied with a reason instead of vanishing or being promoted, an abort resolves both queues, and the runtime never guesses which of the two new input is | Input-algebra property tests over the durable command queue | `apps/loopex/test/input_algebra_test.exs` |
| 4 | The operator gets four working coding tools — `read`, `write`, `edit`, and `bash` — against a real workspace, sharing one conformance suite for bounded output, workspace-root resolution, symlink and path-scope containment, exact edit preconditions and useful mismatch diagnostics, shell-versus-argv semantics, ownership and termination of the whole child process tree, and executor progress that proves its identity, epochs, digest, and fence before anything narrower is projected; every job carries the run's committed absolute deadline instant and runs under an effective deadline that is the earlier of that instant and any wall-time budget the tool itself declares, whose expiry enters the same cancellation sequence and confirms the owned process tree is cleaned before the run commits its bound | Shared tool-behaviour conformance, executor-progress validation negatives, deadline-expiry termination and cleanup confirmation, and real controlled OS execution | `apps/loopex_executor_local/test/coding_tools_test.exs` |
| 5 | Oversized tool output does not poison the conversation: output beyond a tool's declared bound spills through an `ArtifactStore` port to a local adapter, the durable event carries content digest, media type, size, logical role, and an opaque retrieval reference, the model-facing result stays bounded and says what was truncated, and the operator retrieves the artifact by that reference through the public facade | Artifact-store port conformance suite plus spill-and-retrieve tests | `apps/loopex_store_local/test/artifact_store_conformance_test.exs` |
| 6 | Host policy can refuse a tool call: the `Loopex.Policy` port replaces M1's literal allow term, every executor-backed tool including a read-only one requires a decision, a `deny` issues no grant, starts no operating-system process, commits a truthful denied outcome the operator reads, and the run continues or terminates truthfully instead of retrying the refused call; failure, timeout, and a malformed response fail closed into denial and never fall through to allow; a permissive policy applies only where a host names it, omitting the policy refuses runtime start, and the shipped permissive policy says out loud what it is | Policy port conformance, negative authority tests, pre-start refusal proof, and the shipped permissive policy's own lane | `apps/loopex_executor_local/test/host_policy_test.exs` and `apps/loopex_reference_client/test/allow_all_policy_test.exs` |
| 7 | Behaviour-shaping project resources enter the model's context only by an operator's explicit decision: discovery resolves a canonical ordered resource set under declared path, per-file size, and total limits, the operator is shown each path, its provenance, and the manifest digest before deciding, a headless run without a matching positive decision fails closed, a changed workspace, revision, manifest, or content invalidates the decision, an admitted block changes no tool set, policy decision, bound, or grant, and an ordinary workspace read stays a policy-governed tool effect rather than context staging | Discovery and ordering properties, trust-admission negatives, staging-versus-tool-effect separation, and invalidation tests | `apps/loopex/test/project_resource_trust_test.exs` |
| 8 | The operator can stop a running task with Ctrl-C: the interrupt reaches the run through the public facade, scheduling stops, the in-flight model call and executor job are cancelled, and `cancelled` commits only where every owned operation reached a validated terminal fact and every owned process tree was confirmed cleaned; any validated terminal fact that committed first is preserved, anything less ends `outcome_unknown` carrying its reconciliation reference, and the operator is told what was observed | Cancellation protocol tests with process fault injection | `apps/loopex/test/cancellation_test.exs` |
| 9 | The operator finds and continues earlier work: a fresh operating-system process lists the sessions in a state root resolved from `LOOPEX_HOME`, resumes one under the durable runtime placement identity that created it, refuses a resume through a different runtime identity with an explicit reason, and acquires live ownership only through a fresh resume command identity | Session-directory tests and ADR 0008 placement negative tests | `apps/loopex/test/session_directory_test.exs` |
| 10 | `loopex` is a real command: `run` submits a prompt and streams the answer with its tool calls, tool progress, and results, prompt, steer, follow-up, and abort each have a distinct explicit affordance and input naming neither steer nor follow-up is refused, `sessions` lists the operator's sessions, `resume` continues one, `cancel` reconciles a session left behind by a dead process, `artifact` retrieves a spilled artifact by its opaque reference, an interrupt signal cancels the running task through the public facade and reports `outcome_unknown` when cleanup cannot be confirmed, `--policy` selects the host policy that governs the run, and project-resource trust is decided at the terminal; the command surface drives only the public `Loopex` facade, its base system prompt and active tool definitions measure under one thousand tokens before project context, and it takes no external dependency | Command-surface tests, a real interrupt delivered to a real process, source inspection for facade-only access, and a measured prompt budget | `apps/loopex_cli/test/cli_test.exs` |
| 11 | A developer embedding Loopex starts the same kernel in one page: one shipped composition module of at most eighty effective lines starts the OTP application tree and a runtime, names the concrete Store, Model, Executor, Policy, and ArtifactStore implementations, creates a session, submits a prompt, and consumes events, resolves its state root explicitly, and is a copy-and-adapt reference an embedder owns rather than a library it depends on, proved by an independent fixture that composes the kernel without depending on the command application | Measured composition budget and independent embedded end-to-end use | `apps/loopex_cli/test/kernel_composition_test.exs` |

The Gate selector cells identify the protected selector files. The gate contract
locks the complete repository-owned invocation, including role, seed, minimum,
exclusion policy, expected test identities and states, and credential channel.

Two things the gate locks just as hard are deliberately not outcomes. The
runtime-scoped tool registry is the internal mechanism Outcomes 1 and 4 both
resolve through, not something an operator asks for; it is an implementation
constraint with locked supporting coverage. The attended real-provider
demonstration is not a feature either; it is the mandatory closure evidence that
the eleven outcomes above add up to a coding agent, and closure without it is
refused.

Every claim in this milestone that a real provider answered must leave behind
something a person can check outside this repository. A real provider's reply
carries the provider's own identifier for that response and the token usage the
provider reported; the milestone retains both, per real-provider role, beside
the non-secret provider, model, endpoint, and adapter identity it already keeps,
and a case that claims a real provider answered fails when no such identifier is
present. This is stated here with its limit attached, because the limit is the
point: no check that runs offline can prove a network call happened. What the
mechanism achieves is that fabricating one becomes detectable — the retained
identifiers either exist in the provider's account or they do not — and
confirming them there is a closure-review step performed by a person. No plan,
gate, evidence record, or report in this milestone may describe an automated
check as proving a real call occurred.

The runner's own isolation is held to the same standard as the behaviour it
judges. A gate that claims to compile and run outside the checkout must actually
do so under an ambient environment it did not choose, and that claim carries its
own locked definition rather than resting on the runner's prose about itself.

Technical depth: [Mandatory closure evidence, obligations, and mapping](M2-technical.md#technical-plan-evidence).

<a id="concept-plan-scope"></a>
### Scope

Product code intended to survive the milestone: a real multi-turn agent loop
with committed conversation history, declared bounds, and canonical-history
continuation; streaming progress with exactly one durable reconstruction per
turn; the prompt, steer, and follow-up input algebra; a runtime-scoped tool
registry; four conformance-tested bootstrap coding tools; an artifact store for
oversized output with an operator retrieval path; a host policy port with a
working refusal; project-resource discovery and trust admission; working
cancellation; a session directory over a resolved state root; one shipped
composition module; and one operator command application. All of it runs on the
M1 kernel and through the M1 embedded API.

The command surface exposes one operation the outcome list does not derive from
a durable session: `loopex artifact` retrieves a spilled artifact by its opaque
reference. It exists because Outcome 5 promises the operator can read back what
a tool actually produced, and a spill the operator cannot retrieve is a bounded
result that quietly discarded the remainder. It is a projection of the same
`ArtifactStore` port an embedder calls and owns nothing durable.

M2 ships a runnable `loopex` command built as an `escript` from the source tree.
That is an operator feature and is independent of any version number. The root
`VERSION` stays `0.0.0`, nothing is tagged, and nothing is published.

**The boundary behaviours M2 leaves in place are Store, Model, and Executor.**
It adds two narrow ones the founding vision already assigns. `Loopex.Policy` is
the host-control seam that replaces M1's literal `{:host_policy, :allow}` term,
and an `ArtifactStore` port owns oversized tool output so the model-facing
result can stay bounded without discarding what a tool produced. Each ships with
the smallest local adapter and a reusable conformance suite. Neither adds an
application. The tool registry is runtime-scoped data and resolution rules
inside core, not a boundary behaviour.

The `loopex` command is a client application and a peer surface: it owns no
loop, no durable session truth, no cursor truth, no Store access, and no
authority decision. Every operator flow it offers is a projection of the same
embedded API an embedder calls.

Darwin floor, Darwin current, and Linux current are three independent execution
lanes. Each runs the complete gate from a fresh owned root. The floor pair is
Elixir 1.17.0 on OTP 26.0, where neither Erlang's `:json` nor Elixir's `JSON`
exists, so every byte of encoding and decoding this milestone needs is
repository-owned code that runs there.

**Accepting this plan pair takes three decisions.** First, a seventh
application, `apps/loopex_cli`, with role `:client`, and a widened client rule: a
`:client` may declare production dependencies on the in-umbrella `:edge`
applications it composes, and still no external dependency and no dependency on
another `:client`. Second, the two new narrow ports named above, each with one
local adapter and one conformance suite, and no other new boundary behaviour.
Third, two shipped permissive policies rather than one. A `:client` may not
depend on another `:client`, so the command cannot reach the reference client's
and names its own. Both are permission-granting modules an operator can select,
so both are named explicitly, both print the same single notice, both carry
locked coverage, and neither is ever an implicit fallback. The alternative was
widening the client rule to permit a client-to-client dependency, which would
undercut the first decision.

**Accepting this plan pair does not, and cannot, dispose the M1 gate.** M1's
closed gate binds nine files by digest, and M2 must change two of them:
`apps/loopex/lib/mix/tasks/loopex.deps_budget.ex`, whose planned inventory
freezes the repository at exactly six applications with one `:client`, and
`apps/loopex/test/deps_budget_test.exs`, its adversarial corpus, which must gain
the cases that prove the new inventory and the widened client rule. Those
digests are not enforced only by M1's own runner. The repository status check
verifies every plan's declared bound artifacts against the working tree at every
revision and across the history reachable from `HEAD`, with no exemption for a
`Closed` milestone. The moment either file changes, `mix loopex.status` fails,
`bash scripts/check-bootstrap.sh` fails because it runs that check, and the
closed M0 gate goes red because it runs bootstrap. **Until an accepted M1 gate
generation rebinds those two artifacts to the M2 bytes, M0, the bootstrap
aggregate, and the status check are red.** M1 is `Closed`, so that rebinding is
an `amendment-transaction-v2` gate generation rather than the ordinary
amendment: both of M1's authority rows stay byte-immutable and the closed plan
gains one appended generation row. It is a non-delegable baseline exception
requiring the maintainer's explicit disposition; this plan names it, fixes its
shape, and does not execute it.

M1's protection is otherwise carried forward and re-run, not restarted. The M2
gate re-runs M1's eleven protected outcome roles — including both real-provider
roles — at their exact locked identities, states, and minima beside its own.

Technical depth: [Prerequisites and acceptance points](M2-technical.md#technical-plan-prerequisites).

Technical depth: [Ownership and rejoin barriers](M2-technical.md#technical-plan-ownership).

Technical depth: [Packaging mechanics](M2-technical.md#technical-plan-packaging).

Technical depth: [Proportional minimalism budget](M2-technical.md#technical-plan-minimalism).

Technical depth: [Compatibility mechanics](M2-technical.md#technical-plan-compatibility).

Technical depth: [Migration and rollback](M2-technical.md#technical-plan-migration).

<a id="concept-plan-non-goals"></a>
### Non-Goals

**No wire, no daemon, no second client.** The vision's serial barriers put
useful local session and operation truth before multi-client attachment and a
protocol candidate, which they name as one step rather than as two ordered ones.
Splitting that step into a headless protocol milestone and then a daemon
milestone is this project's own sequencing decision, and M2 precedes both. It
stays in the foreground. It creates no JSONL RPC transport, no
line-framed protocol, no golden vectors, no socket, no daemon, no controller
lease, and no second-client attachment contract. A headless JSONL session
protocol is the next milestone's whole subject, and a durable multi-client
daemon is the one after it. Introducing either here would freeze a guess about a
surface no proven client has used yet.

**No cross-process cancellation.** Stopping a live run is Ctrl-C in the process
that is running it. A second `loopex` process cannot cancel the first: ADR 0008
permits one live Runtime Control per `(Store identity, runtime_id)` placement
key, the command re-presents one durable `runtime_id`, and M2 has no
out-of-band channel, so a second process is refused by mutual exclusion rather
than reaching the run. `loopex cancel <session>` is therefore narrow and says
so: it reconciles a session whose owning process died, driving it to a truthful
terminal state from durable evidence. Cancelling another process's live run
needs an attached second client and belongs with the daemon.

**No publication.** Nothing is published to Hex, to a binary release channel, or
to any other distribution point, and no version is tagged. Publishing one
package would permanently create the vision's seventh compatibility surface —
released package names, their contents, and the constraints they declare — which
is inert only while nothing is published. It would also require the name,
trademark, domain, and Hex clearance the roadmap keeps as an unscheduled
question. Neither is worth spending to make a local kernel usable, and both stay
available to a later milestone that has a consumer.

**Three of the seven tools wait.** M2 ships the bootstrap profile — `read`,
`write`, `edit`, `bash`. The `coding_search` profile — `grep`, `find`, `ls` — is
explicitly deferred, because the vision itself splits the surface that way and
asks for measured prompt cost and task utility before an accepted decision fixes
a reference default. The four shipped tools carry the shared conformance the
other three will inherit.

**Refusal now, interactive approval later.** Host policy owns `allow`, `deny`,
and `defer`. M2 delivers `allow` and a working `deny`. A permissive policy is a
host's own decision to trust its own workspace, so each reference host names one
for itself; no port, edge, or core module supplies one, and starting a runtime
with an executor-backed tool and no named policy is refused. `defer` —
interactive per-call approval with a mid-run round trip to the operator — is
explicitly deferred; it needs an interaction contract the foreground rung has
not earned. The project-resource trust decision in Outcome 7 is not a `defer`:
it is settled before a run starts, or the run fails closed, so it introduces no
mid-run interaction.

**No promise that stopping is always clean.** Ctrl-C stops the owned work and
reports what actually happened, which is not the same as always finishing
`cancelled`. A run finishes `cancelled` only where every owned operation reached
a validated terminal fact and every owned process tree was confirmed cleaned;
otherwise it finishes `outcome_unknown` carrying a reconciliation reference. No
document, transcript, or help text in this milestone says an interrupt ends a
run cleanly.

**No context pipeline beyond exact staging.** M2 implements only the initial
local-kernel stage the vision names: canonical history, a fixed reference
project-resource stage, trust admission, total budget enforcement, and exact
receipts. Registered context providers, transformers, selectors, observers,
recall stores, memory, retrieval, embeddings, and prompt libraries are out.

Also out: changing the model within a run, branches and forks, compaction,
extension manifests or activation, isolated or remote hands, an executor broker
or fleet, a durable store selection or migration, a policy engine, sub-agents,
plans, objectives, background jobs, performance budgets, and any public
compatibility freeze. A terminal user interface is out: the command may stay
line-oriented, and only the semantics above are proved.

No checker, registry, document, evidence record, or status row substitutes for
the working loop and the working command. A component that does not serve one of
the eleven outcomes, or the two named mechanisms that support them, is out of
scope.

Technical depth: [Deferral acceptance points](M2-technical.md#technical-plan-prerequisites).
<!-- loopex:plan-concept-envelope:end -->

## Workstreams

Ownership and the required rejoin order are fixed in the
[technical envelope](M2-technical.md#technical-plan-ownership).

- **A — Tool contract and registry.** `loopex_protocol` and core own the tool
  definition shape and the runtime-scoped registry that Outcomes 1 and 4 resolve
  through. Nothing else can name a tool until this exists, so it rejoins first.
- **B — Loop, bounds, streaming, input algebra, and staging.** Core owns
  Outcomes 1, 2, 3, and 7: the turn machine, the canonical history projection,
  exact staging of each turn's request, the three declared bounds, the delta
  algebra and its single durable reconstruction, the prompt/steer/follow-up
  queue, and project-resource staging. It also carries both port arity changes.
  It rejoins second.
- **C — Coding tools, artifacts, and host policy.** `loopex_executor_local` and
  `loopex_store_local` own Outcomes 4, 5, and 6 and the executor half of
  cancellation; `loopex_reference_client` gains the shipped permissive policy
  and its own lane. They implement the registry's contract and rejoin third.
- **D — Cancellation and the session directory.** Core owns Outcomes 8 and 9.
  Cancellation needs C's owned-process termination and B's turn machine, so this
  rejoins fourth.
- **E — Operator surface and demonstration.** `loopex_cli` owns Outcomes 10 and
  11, the attended demonstration, the `escript` entrypoint, and the
  seventh-application inventory change. It rejoins last and proves one operator
  experience through every prior workstream.

## Progress and Evidence

| # | State | Evidence |
| --- | --- | --- |
| 1 | Open | — |
| 2 | Open | — |
| 3 | Open | — |
| 4 | Open | — |
| 5 | Open | — |
| 6 | Open | — |
| 7 | Open | — |
| 8 | Open | — |
| 9 | Open | — |
| 10 | Open | — |
| 11 | Open | — |

## Governance Records

| Decision | Authority | Authority evidence | Bound bytes |
| --- | --- | --- | --- |
| Acceptance | — | — | — |
| Closure | — | — | — |
