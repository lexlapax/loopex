<a id="concept"></a>
## Concept

Technical depth: [Foundation contracts and delivery mechanics](M3-technical.md#technical-depth).

<!-- loopex:plan-concept-envelope:start -->
## Normative Concept Envelope

<a id="concept-plan-purpose"></a>
### Purpose

Make compatible project skills useful through the local runtime, and finish the
core admission, availability and retention repairs needed to extend it safely.
An operator imports a pinned skill, inspects and admits its exact content,
selects it before a run, and uses the existing tool and artifact-inspection
workflow through both embedding and the reference terminal.

Keep M3 and M4 separate. M3 proves this bounded local capability; M4 adds durable
operator interactions, bounded artifact ranges and external protocol consumption.
Those M4 contracts still belong to core and its ports before the wire maps them.
Resource packs do not require trusted OTP extension activation, and the vision's
serial barriers and separate compatibility freezes remain intact.

M2's delays came from product defects found in post-closure audits, broad work
without early integrated proof, and brittle locks. M3 addresses all three:
prove one complete workflow early, audit failure paths across every entrypoint,
and use focused checkpoints with clause witnesses instead of freezing mutable
test inventories. Exact-candidate validation remains required at contract
moments; the Technical envelope records the maintainer's M3-only replacement
for the new inherited aggregate at plan acceptance.

<a id="concept-plan-outcomes"></a>
### Outcomes

| # | Outcome | Evidence class | Gate selector |
| --- | --- | --- | --- |
| 1 | An operator imports a compatible skill directory from an exact Git commit and inspects a complete, bounded project pack without executing downloaded content | Real acquisition, retained-provenance, containment and failure tests | `apps/loopex_composition/test/skill_acquisition_test.exs` |
| 2 | A trusted project catalog and operator-selected instructions/supporting files reach staged model input progressively, with unchanged tools, policy and grants; recovery preserves exact staged bytes | Context conformance, trust negatives, budget/residency properties and model-input capture | `apps/loopex/test/skill_context_test.exs` |
| 3 | Embedded and terminal hosts complete the same skill → tool → artifact-inspect workflow with explicit launch configuration and existing allow/deny policy | Cross-entrypoint conformance, real executor and attended provider demonstration | `apps/loopex_cli/test/foundation_workflow_test.exs` |
| 4 | Required-only context admission, asynchronous dispatcher reads and bounded permit retention meet their accepted contracts while preserving replay and successor fencing | Admission properties, held-Store availability and retention/succession fault tests | `apps/loopex/test/context_admission_test.exs`, `apps/loopex/test/event_dispatcher_availability_test.exs`, `apps/loopex/test/provider_attempt_protocol_test.exs` |
| 5 | Repository enforcement runs inherited gates without recursion at contract moments, verifies override authority before acceptance and protects M2 repair guarantees through stable behavioral witnesses | Invocation and history negatives, protected-witness identity and test-honesty evidence | `apps/loopex/test/history_anchoring_test.exs`, `apps/loopex/test/m3_gate_support_test.exs` |

Technical depth: [Evidence obligations and mapping](M3-technical.md#technical-plan-evidence).

<a id="concept-plan-scope"></a>
### Scope

Reference discovery enumerates only `.agents/skills/<name>/SKILL.md` under the
canonical workspace. A selected Git directory supplies its bounded supporting
files. No home/configured roots, workspace-history discovery, HTTPS single-file
import or traversal of links named by content is included. Installation and
session admission are separate: installation never grants context trust or
execution authority. Trust binds workspace, source/commit and the complete
manifest; changed bytes require a new operator decision.

Admission, skill selection and selection of already manifested supporting labels
are explicit operator/host commands while the session is settled, before a new
run. Each run retains an immutable selection. Model output cannot activate a
skill or issue a resource command. An absent decision withholds content while
ordinary coding continues. Unsupported vendor execution fields are diagnosed;
scripts, hooks, command interpolation and `allowed-tools` remain inert data.
Ordinary tool requests still cross the existing host policy and executor grant
boundary. A hostile pack must leave the tool set, an identical request's policy
result and the grant set unchanged.

M3 adds experimental resource queries and command/receipt variants. Existing
root AGENTS.md discovery, M2 histories and refusal meanings remain unchanged.
New records are versioned; older readers must refuse them before effects.
Rollout uses fresh development roots and retained old root/binary pairs for
rollback, with no in-place downgrade or public compatibility promise. Artifact
inspection uses the existing integrity-checked full-object fetch and its limits;
M3 makes no bounded-range or bounded-allocation claim. The accepted toolchain
pins remain unchanged. There is no release, tag or publication.

Trusted model/executor launch configuration must behave consistently in CLI,
embedding and prepared recovery. M2 provider protection, prepared handoff,
settlement-v2, cleanup grace and Bash prerequisite are inherited guarantees.
M2 remains Closed; skills are new M3 scope.

Amendment 1 adds bounded private provider-failure diagnostics under
[ADR 0029](../adr/0029-bounded-provider-failure-diagnostics.md#concept).
The existing companion may send one finite stage/class pair with an ambiguous
failure terminal. Only the validated terminal-end plus clean-EOF path exposes
that pair within the invocation guardian; optional test observation retains
fixed labels on failure. Raw provider messages and reasons remain unavailable.
The public Model result, dispatch uncertainty, retry prohibition and separate
cleanup proof remain unchanged. Diagnostics do not identify a historical cause
or convert a failed or unrun gate into passing evidence.

This addition uses the existing private channel and invocation lifetime. It
adds no public API, callback option, dependency, application, socket, file
channel, logger output, durable record or production tracing. The five outcomes
remain unchanged; diagnostic conformance supports their inherited provider
boundary and source-built workflow evidence.

Amendment 2 records the [approved shared-selector repair](../developer/agent-context-map.md#override-disposition-m3-selector-diagnostics-2026-09-12) and
[sequential binding checker correction](../developer/agent-context-map.md#override-disposition-m3-sequential-binding-checker-2026-09-12) as test and repository-check
infrastructure for those same outcomes. The exact runner/corpus repair retains
bounded failure identities and categories without arbitrary runtime values;
the checker enforces the separate M1/M2/M3 binding sequence. Only the named
continuing M2 byte restriction is replaced. Historical decisions and evidence
remain true for their revisions. Focused checks govern these transactions;
complete final-candidate gates remain required on macOS and Linux serenity.
This changes no product outcome, ADR, public contract or final pass criterion.

The maintainer-approved
[CLI exception](../developer/agent-context-map.md#override-disposition-m3-cli-extension-ratification-2026-09-10)
preserves all five inherited commands and permits `skill`; it does not require
a separate Closed-M2 amendment transaction. Further inherited restrictions
change only through explicit scoped approval.

Technical depth: [Prerequisites](M3-technical.md#technical-plan-prerequisites).

Technical depth: [Ownership and delivery order](M3-technical.md#technical-plan-ownership).

Technical depth: [Compatibility](M3-technical.md#technical-plan-compatibility).

Technical depth: [Migration and rollback](M3-technical.md#technical-plan-migration).

Technical depth: [Packaging](M3-technical.md#technical-plan-packaging).

Technical depth: [Minimalism](M3-technical.md#technical-plan-minimalism).

<a id="concept-plan-non-goals"></a>
### Non-Goals

Durable policy defer/answer, range reads and the development-floor refresh are
M4 scope. M3 adds no second policy evaluator, interaction record/event family,
new app, production dependency, executor job kind or default tool. Model-driven
skill activation, implicit remote installation, hosted skill APIs, marketplace
search, automatic updates, configurable discovery roots, generic context
providers, daemon lifetime and VM-global plugin activation remain outside M3.
The local framework is the fixed resource seam plus existing tool registration;
no generic plugin loader is required for this capability.

Technical depth: [Decision boundaries](M3-technical.md#technical-plan-prerequisites).
<!-- loopex:plan-concept-envelope:end -->

## Workstreams

One integrator owns the candidate and integration. Contract and prerequisite
settlement precedes the thin integrated workflow; independent repairs may then
proceed in isolated worktrees. Breadth rejoins incrementally behind working
shared contracts. Final self-audit precedes independent closure review.

Planning baseline: `b637873ddc39542ec27add71015b46a4f7c7f80e` (integrated M2).
The previous debt-only draft is preserved at
`d0699b6dc636c7e74b95082d0d8241968381927d`. The original Open planning request
authorized document revisions and gate-tooling cleanup without accepting its
plan, gate or proposed ADRs. Acceptance is now recorded below.
M4 remains an [unopened draft](../archive/M4.md#concept).

The [M2 integrated self-audit](../evidence/M2-ff17990-review-followup.md)
is the concrete basis for the delivery workflow, not an assumption that every
historically reported weakness is still present on the refreshed base.

**Research and scope rationale**

Research reviewed for this proposal on 2026-09-09 distinguishes portable
resource directories, discovery catalogs and hosted services:

- [Agent Skills specification](https://agentskills.io/specification) defines
  SKILL.md metadata, instructions and optional supporting files. Its
  [client guidance](https://agentskills.io/client-implementation/adding-skills-support)
  informs discovery, explicit selection and progressive loading. Compatibility
  is a declared tested subset, not permission to inherit every vendor field.
- [skills.md documentation](https://skills.md/docs) describes hosted execution
  through service interfaces. Downloadable resource packs meet M3's local
  foundation outcome; adding a hosted execution provider is separate scope.
- [skills.sh](https://skills.sh/) and the
  [Vercel installer source](https://github.com/vercel-labs/skills/blob/main/src/local-lock.ts)
  inform distribution and local lock/provenance. The previous blanket claim
  that this installer carries no checksums or provenance is incorrect: its
  lock records source information and a computed content hash.
- [GitHub CLI skill installation](https://cli.github.com/manual/gh_skill_install)
  illustrates explicit sources and pinned revisions. Loopex still makes its own
  acquisition, containment, retention and trust decisions.
- [SkillsMP](https://skillsmp.com/) is a discovery index, and
  [Tessl evaluation](https://docs.tessl.io/evaluate) supplies evaluation signals;
  neither is a runtime authority or substitute for Loopex's negative tests.
- [Claude Code skills](https://code.claude.com/docs/en/skills) include richer
  execution and invocation semantics. Preserve manual-only restrictions and
  diagnose unsupported execution fields; do not import tool permission grants.
- [OpenClaw skills](https://docs.openclaw.ai/skills) illustrates another skills
  and registry ecosystem. Package discovery and trusted code activation remain
  separate decisions in Loopex.

A skill is the first resource-pack capability, not the definition of the whole
foundation milestone. Resource identity, explicit launch wiring and safe core availability/retention
complete this milestone. M4 adds durable interactions and bounded artifact access
at those existing owners before projecting them to external clients.

The implementation checkpoints below are not closure evidence. The reviewed M3
implementation cadence reserves full gates for the final candidate. The reviewed
workflow allocation uses an explicitly identified isolated provider for
deterministic CLI/embedding proof and retains the exact production CLI/companion
claim for the attended lane. Both dispositions and their instructions are
recorded in the [context map](../developer/agent-context-map.md).
M3 remains Accepted and product implementation remains on `m3`.

## Progress and Evidence

| # | State | Evidence |
| --- | --- | --- |
| 1 | Open | Acquisition cleanup, caller-loss fencing, repeated imports and source validation repaired in `720d1d76a28140e69e8acbae30c7b7df36a360a4`. Reviewed contained-root, complete Git inventory, full-pack normalization and restricted-frontmatter repairs integrated in `ee7b8df42193b4db6a322deefc3c08cf266899fe`; all six focused acquisition tests pass at seed 3107. Attended public import and final qualification remain required |
| 2 | Open | Snapshot, commands, progressive staging and replay implemented; resource witnesses in `768525b`; focused trust/budget/recovery tests pass and independent core review found no further blocker. Final candidate evidence remains required |
| 3 | Open | Source-built deterministic workflow in `6ee7ad4`; exact integrated-M2 reader compatibility in `acdcee6`; reviewed configured recovery and cleanup implemented in `a9a142b56dab82aae23834ce0fa97d7516bae3d2`, with 56 CLI and 10 lifecycle/stale-writer tests passing. Fresh-process continued-work proof in `c2ff83decd1daf6b9f8ae96e2ec8a73e45a43547` passes both retained/missing-snapshot cases at seed 3107, observing newly staged provider input after a genuine journal cut and valid workspace edits. Attended production CLI/companion evidence and final qualification remain |
| 4 | Open | Required-first witness in `ae9b8dc`; asynchronous dispatcher repair in `5fb8060`; attach-caller cleanup proof in `d026b35a2991dd5b24c8399522d4cb6fd1030e7e` passes five focused tests. Permit retirement in `16f856808b89a40e94d4e7bd69875076a685ee9e` passes 66 focused tests, including short pages, terminal closure, delayed identities and succession. Independent mutations are caught by the attach-caller cleanup and stale-position witnesses. The three core repair files pass 92 tests on Elixir 1.17/OTP 26, seed 3107. Final qualification remains |
| 5 | Open | Gate tooling and stable witnesses retained; executor stale-root flake repaired in `81eaae5` with causal proof and 73 passing focused tests. Acquisition cancellation now observes actual readiness and holds the Git wrapper until explicit release, preserving its 10-second import deadline and all cleanup assertions; the six owning tests pass on Elixir 1.17/OTP 26, seed 3107. Reviewed implementation-history repair `7995965da0de9a51ace807da4183498142c42cd9` preserves the exact M2-bound corpus throughout reachable history and passes status. Final qualification at `994184dd37956b9cf8d913084461ff1dd6807436` passed the M3 deterministic suite and bootstrap but M0 found an unconfirmed fast-command result in `coding_tools_test.exs` at seed `906860`. A controlled first-probe stall now proves a cleanup-budget starvation condition: the original code exhausted the period before a successor probe could run. The first normal-completion observation now receives the existing cooperative share, leaving recovery inside the unchanged cleanup deadline; positive group and carrier-exit proofs remain required. An otherwise valid first probe slower than that share may be terminated earlier, and repeatedly unavailable probes still report an unknown outcome. The original occurrence remains without causal attribution; this controlled repair and diagnostic passes do not identify its cause. The final inherited aggregate remains required |

## Governance Records

| Decision | Authority | Authority evidence | Bound bytes |
| --- | --- | --- | --- |
| Acceptance | Delegate: m3_independent_review | [disposition](../developer/agent-context-map.md#disposition-m3-amendment-2-acceptance-2026-09-12) | candidate `89a848b6685fd2d6e7d4dfd4768b4d083dc9db36`; concept `sha256:9c004acd94844a9f1369ac84e6b735076c537cc5caabf4ad0e825854334352e5`; technical `sha256:433947d010b8999966a9becb9b260647f2764fa0bbc8ef4448f6b395f0b0d227`; gate `sha256:c129c26a9a7a10edc57f4dbc22e624c70001ee7a780bfef643fc826fdde5ab1f` |
| Closure | — | — | — |
