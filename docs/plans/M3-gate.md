# M3 Gate

Open candidate for project skills and core repairs. No plan or ADR is accepted
by this revision. The runner binds a real behavioral opening probe, executable
closure lanes and exact future witness identities. Under the independently
reviewed maintainer
[preparation-rule ratification](../developer/agent-context-map.md#override-disposition-m3-incremental-witness-ratification-2026-09-10),
future test bodies are written during implementation; every named witness must
pass before closure. The independently reviewed
[acceptance aggregate override](../developer/agent-context-map.md#override-disposition-m3-acceptance-aggregate-2026-09-10)
replaces only the new M0–M2 aggregate at this plan-acceptance transition;
candidate validation and independent review remain required.

The complete command to bind at acceptance is:

```text
bash scripts/check-m3-gate.sh
```

The [Concept plan](M3.md#concept) owns the five outcomes and the
[technical plan](M3-technical.md#technical-depth) owns the contracts and evidence.

<a id="amendment-transaction-v1"></a>

After acceptance, amendments use the direct proposal/rebind transaction unless
a named maintainer override expressly replaces that procedure. M3
changes no floor pin or Closed gate as an opening prerequisite. M4 owns its
future floor proposal and must derive all then-current artifact holders.

## Current Opening Observation

The runner compiles into an isolated root and drives actual sessions using the
real local Store and a deterministic model. Positive controls prove a small
required request dispatches and an admitted project reaches staged input. An
oversized required request proves refusal before dispatch. Adding the same
small admitted project must still refuse at required-only preflight and record
`not_evaluated_required_failure`; the current product instead records optional
byte-budget evaluation before that refusal.

```text
M3 gate RED: required-only context overflow evaluates optional project content before refusal
```

This is a credential-free behavioral red for outcome 4. Scoped OTP tracing
observes actual Store measurements and admission calls in fixture runtime
descendants, with a delivery barrier and positive trace controls. The red
requires an optional-inclusive measurement, not just a disposition label. A
relabel-only mutation remains red. Retained receipts and model-call observations
provide the companion evidence. It does not depend on acceptance state,
exported function names, source text, case counts or an invented receipt member.
Compile/tool failure is UNAVAILABLE (exit 2); a failed positive control is a
named WITNESS ERROR (exit 2); the observed defect is RED (exit 1). Inspection can
pass without a behavior claim. Full mode continues into its closure lanes only
after this probe becomes green. Checkpoint mode retains the observed red while
running the selected diagnostics. A missing future selector or unavailable
dependency is UNAVAILABLE, never a full-gate PASS.

## Readiness Work Before Acceptance

1. Bind the runner, support, outcome manifest and current probe. The manifest
   fixes decisive witness identities for all five outcomes and the dedicated
   attended workflow. It does not freeze every negative case or file count.
2. Verify routing, invalid comparisons, unknown/shared paths, unavailable
   selectors, exact selector-lane accounting, read-only inspection, untracked
   checkpoint inputs, separated Git diagnostics, authoritative reports and
   predecessor invocation. Bootstrap stays a leaf; a synthetic Closed M3 must
   still select only its predecessors, and a runtime sentinel rejects disguised
   aggregate recursion.
3. Prove the exact opening red on the unchanged product base. The current
   admission probe witnesses a repair, not working skills. Undefined future APIs
   and absent fixtures cannot supply that red.
4. Present the complete Proposed ADRs 0025/0027, the reviewed acceptance
   aggregate override and its replacement validation, and this exact candidate
   for independent review and explicit acceptance.

The first implementation checkpoint must prove authorized runtime-control
acquisition → committed provenance → newly launched runtime discovery → admitted
context → ordinary authorized tool → existing full-object artifact use through
both hosts before breadth.
Tests/fixtures are authored with implementation under the bound witness clauses.
Every closure lane is executable now and fails if its required future witness
is missing, skipped, excluded or failing. The approved CLI assertion exception
is the only inherited witness-meaning change in this checkpoint.

## Bound Artifacts

Acceptance binds these runner, manifest, configuration and authoritative-channel
bytes. Product test bodies may grow during implementation; their protected
identities and required state are fixed in `scripts/m3-outcomes.exs`. Existing
Closed gate and bound-artifact bytes remain unchanged.
The M3 gate-support tests follow the same witness rule; their whole-file bytes
are not frozen. The inherited M1 harness corpus retains its existing binding.

| SHA-256 | Path |
| --- | --- |
| `49cf9b8f7fc95846d105e045d86f73110f5de2fde80b9b6214ed30c565b87302` | `scripts/check-m3-gate.sh` |
| `9bf18c61bdcd9292c1ad382ef373fe11acb4764b5535de744f75342c9f85a84a` | `scripts/m3-opening-probe.exs` |
| `4e648aeceadd9098c2132e3b9e169c4c2a01bc1f7e4ff091c34409703a69a34d` | `scripts/m3-gate-support.exs` |
| `c4d485ca3229441c678abe1e8733f90216e89e0dfb9e81786f58f525619aec29` | `scripts/check-closed-gates.sh` |
| `be01eb566ddfdee0de743573e30ec06b9048782a0038e889b04d1da83483ec24` | `scripts/m3-outcomes.exs` |
| `cc290e60d9f9588c75f1259b25976a58d1c30713e570cd5a88c70cdf3c2159a0` | `scripts/m1-exunit-runner.exs` |
| `0a8406ca080c70624e776b01e37c7ded210b54659064cf63723a847a54debe2d` | `apps/loopex/test/m1_exunit_runner_test.exs` |
| `fad47299b27a767785d2a6a776155038054f5457ee3ce0195a37ae667f7a9999` | `.tool-versions` |

## Runner Modes and Evidence Cost

| Mode | Executed role | Evidence meaning |
| --- | --- | --- |
| `--inspect` | Bound artifacts, manifest shape and bootstrap back-edge check; no scratch allocation or source identity | Inspection only; no behavioral PASS |
| `--preflight` | Clean committed-source identity, isolated compile and real-session opening observation | Declared missing behavior or opening-only green |
| `--checkpoint <comparison-SHA>` | Working-source identity, opening and changed-outcome deterministic selectors | Focused diagnostics; retains an opening red after selected checks pass |
| No flag | Opening; once green, every complete lane below | PASS only after all closure obligations execute successfully |

Checkpoint results are focused diagnostics, never acceptance or closure proof.
The accepted checkpoint role takes an explicit retained comparison SHA, selects
all deterministic outcomes on shared/unclassified product changes, and includes
relevant untracked work. Invalid or unavailable comparison is not an empty diff.
The comparison is a complete 40-character commit SHA retained as an ancestor of
`HEAD`. The executable path map lives in the bound support script. Relevant
untracked work participates in selection; Git diagnostics remain outside the
machine-readable output. An invalid comparison never becomes an empty change
set. Exact `HEAD` with no working change prints a distinct no-outcomes diagnostic
and returns the opening result; it cannot print a selected-outcomes success.
An opening red stops full/preflight mode. Checkpoint mode continues its selected
diagnostics, returns 1 if they pass while the opening remains red, and returns 2
if a selected witness is unavailable. Before either a checkpoint-success line
or full-gate continuation, an exact ordered selector ledger must account for
every manifest-derived lane. Both observations remain visible; neither result
is acceptance or closure evidence.

The reviewed M3 acceptance aggregate override waives a new aggregate only at
this plan-acceptance transition; record it as explicitly waived, never PASS or
unavailable. Run the inherited aggregate at every parallel-workstream rejoin,
every rebind child, closure candidates, at least once in each UTC day when
dependent product work lands on M3, and whenever changed bytes invalidate its
later evidence. There is no requirement to rerun the two-hour
inherited set on every commit; that duration is the supplied review's machine
observation, not a timing promise. An omitted scheduled or rejoin run is
unavailable evidence, and an observed red blocks. Unknown acceptance impact
fails closed to the full gate. Protected-selector execution never invokes the
aggregate itself.

The shell keeps byte-counted parsing under `LC_ALL=C`; every Elixir and Mix child
runs under `C.UTF-8`. Full and preflight roles require one clean committed source
identity. Checkpoint mode binds tracked and untracked working bytes at entry and
rechecks them before its result. Once the opening red is repaired, full mode
refuses a missing provider frame before dependency materialization or closure
lanes. Bootstrap runs with a private activity sentinel that the aggregate
records before refusing. The gate rejects that invocation ledger even if
bootstrap swallows the child exit, so variable-expanded or otherwise disguised
bootstrap recursion cannot execute or disappear.

## Required Full Lanes

| Lane | Complete executable obligation |
| --- | --- |
| Inspection | Artifact identity, exact outcome manifest and bootstrap topology; full status/pairing checked by bootstrap |
| Opening | Isolated compile; real Store/session admission-order observation with positive controls |
| Inherited | Bootstrap and all required Closed commands with credential lanes and truthful propagation |
| Protected outcomes | Standalone authoritative ExUnit result channel, seed 3107, exact required witness identities and runnable states |
| Whole suite | Complete deterministic suite, format, warning-free compile, documentation and dependency checks |
| Real workflow | Separately selected attended public anonymous HTTPS Git acquisition and real-provider skill/tool/artifact task |
| Retained evidence | Execution output binds source/gate/commands/seed, toolchains/platforms and non-secret build/artifact identities; save that output without relabelling its source |

Real-provider tests live in the dedicated
`apps/loopex_cli/test/foundation_workflow_real_test.exs`. Full mode accepts only
the bounded stdin frame `LOOPEX_M3_PROVIDER_V1\0<key>\0` (key at most 16,384 bytes);
the unexported value reaches only the real selector and the existing required
inherited credential lanes. Diagnostic roles accept no provider input. The
aggregate emits each predecessor's existing input format; it does not change
that predecessor's credential contract. No mixed-file exclusion inventory is locked.
The inherited real-provider lanes use the reference model
`anthropic:claude-haiku-4-5`; the credential must belong to that Anthropic
provider. Confirm the pinned model/provider before supplying a key. The
provider-neutral input variable does not make other providers' keys compatible.

## Protected Outcome Obligations

| Outcome | Protected selector family | Required clauses |
| --- | --- | --- |
| 1 | `apps/loopex/test/resource_acquisition_test.exs`, `apps/loopex_executor_local/test/resource_acquisition_test.exs`, `apps/loopex_composition/test/skill_acquisition_test.exs` | One-open-operation and `acquisition_busy` behavior; every ADR 0009 allow/denial/defer/absent-policy row with zero effect outside valid allow; every no-effect, pending-policy, attempt-open, reconciliation, terminal and quarantined transition; acquisition-version CAS and deterministic transaction recovery; exact acquisition/reconciliation/status input, reply, validation, replay and conflict behavior; exact command, intent, request, grant, attempt, query, local admission/open, retained hand response, result, status, terminal and receipt member sets, canonical skill-source/file-set/publication/request/grant/receipt/reconciliation-response digests, reason/nullability relations and replay; Store fault cuts; host root/ref/lease/original-attempt executor/current-responder executor/fence binding; responder supersession and no-call replay after route change, restart reconstruction, exact admission-without-open non-dispatch proof, in-flight retention, terminal settlement, immutable unknown, post-unknown process-absence release without cleanup, and stale/unsolicited refusal; guarded Git through closed counted anonymous-HTTPS transport with exact deadline/grant-expiry equality, overflow refusal, and every budget; exact destination name/commit/tree/file identity and containment; fixed-parent creation/sync/races and collision-proof outside-discovery same-filesystem staging; atomic prepared-slot contention, existing/racing-destination and no-replace behavior; lost-acknowledgement recovery; separate acquisition/use runtime; committed-only provenance; no hooks, scripts or credentials |
| 2 | `apps/loopex/test/skill_context_test.exs` | Catalog/instruction/supporting order; operator-only settled-state commands and in-place same-skill selection replacement; complete paginated trust view/final-page confirmation; canonical-or-nil secret-free repository origin; complete fresh-process literal-directory/entry/file-set attestation that detects additions/removals, with directory/frontmatter name equality and no retained-snapshot shortcut; manifest-only labels and explicit SKILL.md refusal; trust invalidation; hostile-pack tool/policy/grant/acquisition-state invariance; canonical model-visible message framing; exact revision-3 receipt/status/detail/nullability relations; whole-block withholding without trimming while later blocks may fit; exact-count reservation, fixed first-failure order, class arithmetic, every legal admission dimension and unreachable `resource_class_tokens`; zero-or-one single-workspace runtime snapshot with durable host sync/reopen before immutable `resource_snapshot_binding_v1` commit or match, commit-unknown retention, resume reconstruction, exact match/changed/unavailable attestation, recovery-only negative authority and source-free settlement, new-runtime requirement for changed bytes and identity-only session residency; retained staged-input identity without refetch/ambiguous redispatch. Outcome 3's two host integration witnesses below prove this row's pagination and filesystem-discovery clauses through the actual host/CLI boundary. |
| 3 | `apps/loopex_cli/test/foundation_workflow_test.exs` | Same embedded/CLI administrative-acquisition and session semantics; crash-safe pre-submit recovery handle and fresh use-runtime/trust boundary; source-built CLI/companion; trusted launch/recovery; safe terminal rendering and structured headless review; byte-identical ordinary ADR 0007 job/grant/event/receipt/reconciliation/oracle and ADR 0009 request/decision/context alongside acquisition; genuine M2 positive replay and old-reader refusal for every new root/record family; actual authorized ordinary tool and existing full-object artifact integrity |
| 4 | `apps/loopex/test/context_admission_test.exs`, `apps/loopex/test/event_dispatcher_availability_test.exs`, `apps/loopex/test/provider_attempt_protocol_test.exs` | Required-only order/fixed-point properties and old refusal replay; held-Store availability/publication fencing; bounded retention with retired-identity and successor refusal, including run-terminal-only work without settlement retained until session release |
| 5 | `apps/loopex/test/history_anchoring_test.exs`, `apps/loopex/test/m3_gate_support_test.exs` | Override citation and standalone-anchor ancestry across ordinary integration merges, including laundering refusal; register-derived predecessor commands including a later `Blocked` row; missing/red/omitted invocation; runtime bootstrap back-edge refusal; synthetic M3 closure; checkpoint mapping including untracked work and empty exact-HEAD diagnostics; separated Git output; exact selector-lane accounting; representative repair-witness integrity |

### Witness-to-clause allocation

The exact names below are the names already bound by `scripts/m3-outcomes.exs`.
Each allocated clause needs its own observed assertion inside that named case; a
passing case title alone is not evidence.

| Outcome | Exact protected witness name | Allocated clauses |
| --- | --- | --- |
| 1 | `runtime acquisition commits command intent and bound attempt before the authorized hand starts` | One-open-operation and `acquisition_busy`; accepted command/intent before policy; exact ADR 0009 valid allow/five-category denial/defer/invalid-failure/absent-policy mapping with zero grant, attempt or hand outside valid allow; acquisition-version competing-Control CAS; deterministic transaction identity; request/grant/attempt ordering |
| 1 | `lost replies open effects and Store commit unknown reconcile without blind retry or false terminal truth` | Exact acquisition/reconciliation/status input/reply/validation/replay/conflict behavior; exact command, intent, request, grant, attempt, local admission/open/authority, reconciliation query/retained hand response/result, status, terminal and receipt member/digest/nullability/replay matrices; atomic admission/active-authority creation and conditional open replacement; separately bound original-attempt and current-responder lease/executor epochs and ledger-root identity; every Store cut; restart reconstruction from retained response; responder supersession, no-call replay after route change, and late old-epoch refusal; query/result attempts and fences unchanged; locked active→revoked transition plus admission-without-open as the sole non-dispatch proof; race a paused old hand against non-dispatch retry and require every later mutation to fail; in-flight retention; terminal settlement; immutable unknown; locked process-absence plus authority revocation before post-unknown release; race a paused hand against release and require no later publication; stale, duplicate and unsolicited refusal |
| 1 | `the local hand one shot guard binds canonical grant lease executor epoch fence budgets and exact anonymous HTTPS Git identities` | Exact repository-owned C11/libc Port-helper source, compiler identity and source-built package placement; Darwin-floor `renameatx_np(RENAME_EXCL)` and Linux-current `renameat2(RENAME_NOREPLACE)` atomic-race self-probes; Port isolation and unsupported-helper refusal before mutation; final request/grant/root/lease/executor/epoch/fence validation; exact deadline/grant-expiry equality and overflow/elapsed refusal; one-shot release; canonical public source plus staging/skill-source/file-set/request/grant/receipt/reconciliation-response identities; exact-authority counted HTTPS transport with closed global-unicast classification, complete answer-set pinning, hostname-preserving TLS, private/reserved/mapped/mixed-answer/rebinding negatives and zero guard release; every resource budget; safe absent-parent creation, race/sync/reopen identity, collision-proof same-filesystem staging outside discovery; containment; no downloaded hook, script, filter, credential or other content-directed effect |
| 1 | `publication cuts and competing prepared attempts never replace a destination preserve prior committed provenance and attach remote identity only after verified Store terminal` | Canonical publication identity; source runtime/operation provenance join; atomic prepared-slot contention and exact-owner cleanup before unknown; no-replace publication; existing and racing destination preservation; attempt-owned absent-or-complete cuts; CLI lost-acknowledgement locator/replay without another Git effect; committed-only provenance and prior-generation preservation |
| 2 | `catalog selected instructions and manifested supporting blocks stage in durable order` | Catalog/instruction/supporting order; canonical model-visible block framing; manifest-only labels; explicit `SKILL.md` supporting-label refusal |
| 2 | `only settled operator commands change the next run selection` | Operator-only settled-state admission/selection; mid-run and model-origin refusal with zero acquisition-policy, hand or executor calls; repeat idempotency; same-skill supporting-label replacement in its original slot; four-skill limit |
| 2 | `changed or revoked manifest identity withholds resources without stopping ordinary coding` | Under a matching normal-mode launch, core-side admission/trust/retained-content decision, manifest, workspace and digest mismatches withhold resource blocks while ordinary coding continues. Fresh-attestation changed/unavailable launch belongs only to the snapshot witness. Host pagination and discovery are allocated to the Outcome 3 cases below. |
| 2 | `hostile skill metadata changes neither tool registry policy result grants nor administrative acquisition state` | Hostile `allowed-tools`, hook, script and vendor-field invariance across tool registry, identical ordinary-policy result, ordinary grants and administrative acquisition state; admission/activation creates no acquisition policy call, hand call or executor intent |
| 2 | `resource admission reserves exact candidate receipt metadata and applies every fixed first failure dimension before resource reads or dispatch` | Exact revision-3 receipt header/block status-detail-nullability relations; zero-through-37 exact-count reservation; 8 KiB member bound; fixed first-failure order; byte/token/depth/cardinality/record arithmetic; whole-block withholding without trimming while later blocks may fit; unreachable durable `resource_class_tokens`; zero optional reads/calls at the required cuts |
| 2 | `recovery preserves exact staged resource bytes without refetch or ambiguous redispatch` | Retained staged bytes and descriptors recover without project/network reread, snapshot substitution or provider redispatch; missing retained content declines only the resource class |
| 2 | `one runtime core snapshot serves one workspace while sessions retain only identities and staged bytes` | Both zero- and one-snapshot runtime cases; durable normalized host snapshot sync/reopen before immutable `resource_snapshot_binding_v1` create/match, retention across unknown commit and digest reconstruction on resume; transaction recovery before child start; unused-ID/retained-state serialization; exact current-attestation member/state/nullability relations with no acquisition fence member; normal exact match; changed/unavailable recovery-only with zero new run/resource boundary calls and source-free staged settlement; nil/non-nil refusal and changed-byte/new-runtime use; one workspace binding; host/core residency and transient two-copy startup bound; no complete per-session or second-workspace copy; shutdown ownership |
| 3 | `embedding and the source built CLI use the same administrative acquisition skill tool and full artifact workflow` | Host/CLI complete paginated trust display; canonical escaping of all pack-controlled terminal fields including ANSI, C0/C1 and bidi-hostile values with constant prompt bytes outside them; structured headless review; exact manifest/provenance/file rows; unsafe raw repository origin omitted and invalid nonnil core origin refused; final-page and exact-digest confirmation; EOF/refusal/changed-digest no-admission; crash-safe lost-acknowledgement recovery; distinct fresh use runtime and new trust; byte/member-identical ordinary ADR 0007 job/grant/events/receipt/reconciliation/oracle and ADR 0009 tool-policy request/decision/context with no cross-family member; equivalent explicit headless confirmation |
| 3 | `trusted launch and fresh process recovery preserve resource and provider configuration` | Literal `.agents/skills` identity and complete immediate entry set; complete bounded per-pack file walk; directory/frontmatter name equality; added/removed/renamed/replaced/file-changed detection; no retained-snapshot or destination-only shortcut; exact current-attestation match/changed/unavailable construction under the current workspace lease plus durable snapshot-binding match and recovery-only routing before launch/recovery; acquisition fence remains a separate attempt binding |
| 3 | `new readers preserve genuine M2 history and old readers refuse new records before effects` | Genuine M2 runtime/session history replays byte-identically with nil resource envelope, no snapshot-binding record and every resource API unavailable; a retained M2 identity refuses in-place resource enablement; the retained M2 reader refuses before effects on an M3 snapshot-binding root, any acquisition record/root, and every accepted or refused-only `resource_command_v1` session |

The first two Outcome 3 rows above are the host-boundary half of Outcome 2 and run in
the same full gate. Outcome 2's core witness receives canonical plain data and
cannot substitute for those filesystem, CLI or headless-host observations.

Each required clause maps to a named decisive witness in `scripts/m3-outcomes.exs`.
Related clauses may share one named case only when it contains distinct observed
assertions for them. Additional ordinary-suite negatives remain required to pass but their
names and whole-file counts are not locked. An integrated witness can serve
multiple clauses only with distinct observable assertions. Canonical fixture,
harness and result-channel bytes are digest-bound; mutable test files are
protected by witness identity and required state. No protected witness may be
removed, renamed, skipped or excluded without an accepted amendment or an
explicitly approved scoped override. Existing Closed locks retain their meaning
with the recorded successor CLI exception.

## Closure Evidence and Review

Closure is evidence for the five product outcomes, not a sixth process outcome.
Every negative proves its boundary was reached. Mutation attacks the clause and
sibling transition; preserve legacy assertions. Repeated findings trigger a
root-cause audit across entrypoints. Integrated implementer self-audit precedes
independent exact-source review. Neither actor accepts its own result.

Freeze source S and retain genuine old-format positive controls, old-reader
refusal of new resource records before effects, actual build/platform proof,
real workflow evidence and the seven documentation rows. Evidence-only children
name S; they do not relabel earlier results as later runs. Relevant byte changes
invalidate affected evidence/review; unknown scope requires the full gate.
A red or unresolved blocking finding blocks closure. A disappearing same-source
failure is a blocking flake. No fixed review-round or runtime promise is made.

## Documentation Obligations

| Category | Required closure disposition |
| --- | --- |
| Operator-facing documentation | `docs/operator/coding-sessions.md`, `docs/operator/tools-and-policy.md`, `docs/operator/how-a-run-works.md`, `docs/operator/how-a-run-works-technical.md` |
| Operator README | `docs/operator/README.md` |
| Developer-facing documentation | `DEVELOPMENT.md`, `docs/developer/runtime-and-embedding.md`, `docs/developer/agent-loop-and-tools.md`, `docs/developer/architecture.md`, `docs/developer/architecture-technical.md`, `docs/developer/compatibility-surfaces.md`, `docs/developer/agent-context-map.md` |
| Developer README | `docs/developer/README.md` |
| Documentation README | `docs/README.md` |
| Root README | `README.md` |
| Changelog | `CHANGELOG.md` |
