<a id="concept"></a>
## Concept

Technical depth: [Milestone mechanics](M1-technical.md#technical-depth).

<!-- loopex:plan-concept-envelope:start -->
## Normative Concept Envelope

<a id="concept-plan-purpose"></a>
### Purpose

Make Loopex usable through one coherent single-machine trace. An embedded caller
starts an explicit runtime, creates or resumes a durable session, sends a real
prompt to a real model, executes one host-granted controlled local tool, and
observes committed events. After an external harness untrappably kills the
runtime's BEAM OS-process tree at the catalogued point after durable receipt
retention but before the session fact commits, a newly started process resumes
the same session, reconciles the effect without dispatching or performing it
again, makes the next real model call, and reaches a durable terminal result.

M0 proved isolated feasibility properties. This milestone turns the minimum set
needed for that trace into product code and claims no more than one machine and
one attached caller. The same source and gate must execute on Darwin under both
ADR 0002 validation pairs and on Linux under the current pair. This is milestone
execution evidence, not a released operating-system compatibility freeze or a
claim about the untested floor-on-Linux combination.

<a id="concept-plan-outcomes"></a>
### Outcomes

| # | Outcome | Evidence class | Gate selector |
| --- | --- | --- | --- |
| 1 | An explicit supervised runtime starts and stops; two independently configured runtimes coexist in one VM, and no runtime reference or instance state is discovered through a global name or application environment | Runtime isolation and negative tests | `apps/loopex/test/runtime_test.exs` |
| 2 | The embedded API creates a session through a catalogued runtime-control transaction in the Store boundary that atomically commits session-ID allocation, `(runtime_id, command_id)` resolution and mapping, and genesis; identical creation re-presentation returns the same session ID while changed canonical bytes conflict; an initial or resumed coordinator admits no session command until ADR 0006 `advance_owner` commits, one coordinator then serializes the session, at most one run is active, and a superseded owner cannot newly commit or use a delayed result to update the current cache, publish, dispatch, or authorize work | Generated histories, run-admission tests, and ownership fault injection | `apps/loopex/test/session_lifecycle_test.exs` |
| 3 | An in-memory test implementation and a durable local store share ADR 0006's atomic transaction, ownership, version, transaction-resolution, and outbox semantics; only the durable implementation claims persistence, restart, and replay, where replay audits committed history and never substitutes for commit-time fencing | Layered reusable conformance and derived fault injection | `apps/loopex_store_local/test/store_conformance_test.exs` |
| 4 | The embedded API exposes runtime and session lifecycle, commands, race-free snapshot-plus-subscription, and event consumption as one semantic surface; committed public events come from the durable outbox without gaps, while transient progress and diagnostics remain separate; each attachment has a finite configured queue, and overflow disconnects that attachment with its last stable position in committed durable public-event history rather than blocking session truth | Boundary contract, attachment-race, backpressure, and restart tests | `apps/loopex/test/embedded_api_test.exs` |
| 5 | Deterministic and ReqLLM adapters satisfy one model boundary, and an explicitly invoked real-provider role proves that the production adapter received the exact canonical request bytes and digest already committed for that session and completed the real call; the credential-free conformance role is supporting evidence, not a substitute for that real-path assertion | Adapter conformance plus retained session-integrated real path | `apps/loopex_llm_reqllm/test/real_model_lane_test.exs` and `apps/loopex_reference_client/test/real_model_session_test.exs` |
| 6 | Only an explicit host-policy allow decision issues a grant; a trusted-local executor independently recomputes the request digest, applies ADR 0007's production-derived validation at the final serialized pre-start boundary, holds the workspace lease for the job, retains a bound receipt, and launches one real controlled local tool as a separate OS process with an explicit credential-free environment; the independent governed oracle is complete, and every missing or present-but-wrong required binding is refused before the effect | Issuance and lifecycle conformance, schema equality, generated negative corpus, and real execution | `apps/loopex_executor_local/test/executor_test.exs` |
| 7 | A thin reference client drives the working loop through the embedded API only and owns no alternate loop, authority decision, durable session state, or event truth | API-only end-to-end demonstration | `apps/loopex_reference_client/test/reference_client_test.exs` |
| 8 | One real-provider recovery trace itself uses provider-supported deterministic selection of a real controlled OS tool, proves the tool child receives no provider credential, untrappably kills the runtime process tree after durable receipt retention but before the session fact commits, resumes in a new process, admits the receipt only through current reconciliation, proves exactly one logical dispatch and effect, loses no acknowledged fact, makes the next real model call, and reaches a durable terminal result; deterministic tests support but do not substitute for that trace, and the companion fault where an effect may have occurred without a provable receipt ends `outcome_unknown` and is never blindly retried | OS-process fault injection, counted dispatches and effects, and retained real-path demonstration | `apps/loopex_reference_client/test/end_to_end_recovery_test.exs` |

The Gate selector cells identify the protected selector files. The gate contract
locks the complete repository-owned invocation, including role, seed, minimum,
exclusion policy, expected test identities and states, and credential channel.

Technical depth: [Evidence obligations and mapping](M1-technical.md#technical-plan-evidence).

<a id="concept-plan-scope"></a>
### Scope

Product code intended to survive the milestone: one machine, one attached
caller, one active run per session, explicit runtime references, a durable local
store, a provider-neutral model boundary, a trusted-local executor, and a direct
embedded API. An in-memory implementation supports deterministic conformance
tests for the store's live transaction semantics; it is not presented as a
durable embedding mode. The durable implementation alone proves persistence,
restart, replay, and reconciliation.

Darwin floor/current and Linux current are three independent execution lanes.
Each runs the complete gate from a fresh owned root; no lane may substitute
prose, a foreign toolchain, or another lane's build output. Linux coverage proves
the tests and working trace are runnable there at the exact current pair. It
does not widen M1 into packaging, installation, or a public platform contract.

Attachment state is runtime-local rather than persisted. A resume cursor is a
stable position in committed durable public-event history, remains valid across
runtime restart, and lets the same caller reattach without a gap; it is not a
durable subscriber or attachment record.

The milestone has exactly three product boundary behaviours: store, model, and
executor. The embedded API is a direct runtime facade, not a fourth replaceable
boundary. The host owns policy and issues grants; Loopex coordinates durable
mechanics; the executor alone starts the controlled effect after final
validation.

M0 code is reused only where its behavior conforms to the accepted M1 contracts
and the working trace needs it. M0 journals and retained evidence are not an
installed data source or a compatibility promise for M1.

Technical depth: [Prerequisites and acceptance points](M1-technical.md#technical-plan-prerequisites).

Technical depth: [Ownership and rejoin barriers](M1-technical.md#technical-plan-ownership).

Technical depth: [Packaging mechanics](M1-technical.md#technical-plan-packaging).

Technical depth: [Proportional minimalism budget](M1-technical.md#technical-plan-minimalism).

Technical depth: [Compatibility mechanics](M1-technical.md#technical-plan-compatibility).

Technical depth: [Migration and rollback](M1-technical.md#technical-plan-migration).

<a id="concept-plan-non-goals"></a>
### Non-Goals

Multi-client attachment, a portable protocol candidate, a daemon or network
transport, distribution across nodes, isolated or remote hands, an executor
fleet or broker, extension manifests or activation, package publication, an
installed-store migration, and any public compatibility claim.

The trusted-local grant is checked for exact binding but is not claimed to be
unforgeable, tamper-evident, or safe to transport. The durable local store proves
the accepted port on one machine; choosing PostgreSQL, SQLite, or another future
adapter is outside this milestone.

VM code generation remains M0 evidence rather than production extension work.
Nothing here promotes the spike to quiescent activation, atomic module-set
loading, or rollback of a trusted code generation.

No checker, registry, generic transport, or framework layer may stand in for the
working trace. A component that does not serve one of the eight outcomes is out
of scope.

Technical depth: [Deferral acceptance points](M1-technical.md#technical-plan-prerequisites).
<!-- loopex:plan-concept-envelope:end -->

## Workstreams

Ownership and the required rejoin order are fixed in the
[technical envelope](M1-technical.md#technical-plan-ownership).

- **A — Store and durable truth.** The Store port stays in core;
  the `loopex_store_local` edge application owns the local implementations and
  Outcome 3 conformance. This workstream supplies the durable ownership basis of
  Outcome 2 and rejoins first.
- **B — Runtime and embedded API.** Core owns Outcomes 1, 2, and 4. It builds
  only on the integrated store contract and rejoins second.
- **C — Model and executor boundaries.** The `loopex_llm_reqllm` edge
  application owns the adapter-conformance part of Outcome 5, and the
  `loopex_executor_local` edge application owns Outcome 6. They implement their
  inward contracts after the runtime/API path rejoins and then rejoin third.
- **D — Client and combined demonstration.** `loopex_reference_client` owns the
  session-integration part of Outcome 5, Outcomes 7 and 8, and the
  test-only composition of the concrete adapters. It rejoins last and proves
  one trace through all prior workstreams.

## Progress and Evidence

| # | State | Evidence |
| --- | --- | --- |
| 1 | Open | — |
| 2 | Open | — |
| 3 | Open | — |
| 4 | Open | — |
| 5 | Open | — |
| 6 | Open | — |
| 7 | Open | — |
| 8 | Open | — |

## Governance Records

| Decision | Authority | Authority evidence | Bound bytes |
| --- | --- | --- | --- |
| Acceptance | Maintainer | [disposition](../developer/agent-context-map.md#disposition-m1-amendment-4-2026-08-22) | candidate `771d847ab5b186f4552f294f78ffb63e5c7fca72`; concept `sha256:dad88ff975fd5f9a418a279c58f321d41b92cc52842775465daac190eb0bcf62`; technical `sha256:163275593815b801ca0dd13e4b34f57cd426134ee1354695393fae6fa7b8a73b`; gate `sha256:618ab2256db05f689d7513bccde5dafa5e22f59b6d14880ddcdd6876dec8c482` |
| Closure | — | — | — |
