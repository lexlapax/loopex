# Locked M0 toolchain matrix. These exact bytes are digested in
# docs/plans/M0-gate.md and verified against this file by the status check.
# Two pairs are validated individually; no cross-product is constructed.
# Changing any version requires an amendment to ADR 0002, not a gate edit.
#
# Verified against the official Elixir compatibility table on 2026-08-16:
# 1.20 supports OTP 27-29, and 1.17 supports OTP 25-27.
#
# Pins are derived from accepted ADR 0002, not chosen. The floor is the lowest
# supported pair in the 1.17 family, and the table offers no patch selector, so
# the lowest patch of each is taken. The current pair is the newest supported
# release, which is also Homebrew stable today. The floor lane predates what
# brew carries and runs through a version manager or CI; the gate requires both
# lanes recorded, not both run locally on every invocation.
#
# floor pair
elixir 1.17.0-otp-26
erlang 26.0
#
# current pair
elixir 1.20.3-otp-29
erlang 29.0.5
