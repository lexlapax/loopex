interface:
  display_name: "Milestone Gate"
  short_description: "Open a milestone with its plan pair"
  default_prompt: "Use $gate to write a milestone plan pair and register it."
policy:
  allow_implicit_invocation: false
